<h2 class="text-center mt-2">Blog Categories</h2>

<!--Utilizing the will_paginate gem to include pagination in the rendering of the categories listing - just becuase ... -->
<div class="apple_pagination">
  <%= will_paginate @categories, :container => false %>
</div>

<%= render 'categories/category' %>

<div class="apple_pagination">
  <%= will_paginate @categories, :container => false %>
</div>
