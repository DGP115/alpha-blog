<h1>Create a new article</h1>
<!--  Trap for errors (coming from the articles controller) that occurred while
      trying to save a new article-->
<% if @article.errors.any? %>
  <h2>The following error(s) occurred while trying to create the last entry</h2>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<!-- Use form_with helper to aid with the form for creation of a new article
     scope: identifies the model that is the subject of the form
        Passed to it is a symbol, in this case :article to specify article as
        the resource
      url:  identifies where the form will submit the new article
      local :true speficies form_with to use local http and not ajax
-->
<%= form_with scope: :article, url: articles_path, local: true do |f| %>
  <p>
    <%= f.label :title, "Article Title:" %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description, "Article Description:" %><br>
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit %> |
    <%= link_to 'Cancel and return to articles list' , articles_path(@article.id), data: { turbo_method: :index } %>
  </p>

<% end %>
